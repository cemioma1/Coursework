def addMatrix(a,b):
    c = []
    for i in range (0,len(a)):
        c.append([])
        for j in range(0,len(a,[i])):
            c[i].appendd(a[i][j] + b[i][j])

    return c

matrix1Input = input("Enter Matrix 1: ")
matrix1data = matrix1Input.split
a = [int(x) for x in matrix1data]

matrix2Input = input("Enter Matrix 2: ")
matrix2data = matrix2Input.split
b = [int(x) for x in matrix2data]

addMatrix(a,b )